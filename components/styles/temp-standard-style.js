import { StyleSheet } from "react-native";

const Standard = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "flex-end",
    marginBottom: 36,
    backgroundColor: "#003",
  },
});

export { Standard };
